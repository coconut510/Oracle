@ Function
-> 프로시저와 거의 유사한 용도로 사용하지만, 실행 결과를 되돌려(return)
받을 수 있다는 점에서 플고시저와 다름
-> 리턴 값이 반드시 존재하는 객체

@ 함수 작성 및 실행 방법

CREATE [OR REPLACE] FUNCTION 함수 이름(매개변수명 자료형, 매개변수명 자료형...)
RETURN 자료형;
IS
	지역변수선언;
BEGIN
	실행할 SQL문;
	RETURN 데이터;
END;

* 주의 : 매개 변수와 리턴 자료형에는 크기를 꼭 지정하지 마세요